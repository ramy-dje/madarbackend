// FileType can still be useful for broad categorization/UI icons
export enum FileType {
  IMAGE = 'image',
  DOCUMENT = 'document',
  PDF = 'pdf',
  VIDEO = 'video',
  AUDIO = 'audio',
  ARCHIVE = 'archive', // Added for zip, gz etc.
  OTHER = 'other',
}
